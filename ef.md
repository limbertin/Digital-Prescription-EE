"Clipboard to File" $fileName = input("Enter filename (without extension)"); if ($fileName == "") {return}; $fileType = input("Select file type", "", "Choose File Type", "c|TXT|MD"); if ($fileType == "") {return}; $ext = ($fileType == "1") ? "txt" : "md"; $finalName = $fileName . "." . $ext; $result = writefile("<curpath>\\" . $finalName, "<clipboard>", "r"); focus(list); #485; status "File created: " . $finalName;
